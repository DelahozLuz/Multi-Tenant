# SQL Server (Docker)
MSSQL_SA_PASSWORD=S3cur3P@ssw0rd!
MSSQL_DATABASE=multi_tenant_db

# JWT (compartido local y Docker)
JWT_KEY=SuperSecretKey123456789012345678901234567890
JWT_HOURS=8

# Frontend API URL
# "/api" funciona con Nginx (Docker) y con proxy.conf.json (local)
# Cambiar a "http://localhost:5000" si se conecta directo sin proxy
API_URL=/api
